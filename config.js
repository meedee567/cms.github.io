/* ==========================================================================
 * ค่าตั้งต้นที่แก้ได้ "หลัง build" — ไม่ต้องคอมไพล์ใหม่
 * ==========================================================================
 *
 * ทำไมต้องมีไฟล์นี้
 * -----------------
 * Next.js แทนค่า NEXT_PUBLIC_* ลงในไฟล์ JS ตั้งแต่ตอน build แล้วตั้งชื่อไฟล์
 * ตามแฮชของเนื้อหา เช่น  _next/static/chunks/18-gjhkkm79p6.js
 *
 * แปลว่าถ้าแก้ .env แล้วลืมสั่ง build ใหม่ ค่าเดิมจะยังฝังอยู่ในไฟล์ที่อัปโหลดไป
 * อาการที่เจอคือ "กดปุ่มแล้วไม่มีอะไรเกิดขึ้น" ทั้งที่แก้ .env แล้ว
 * ซึ่งหาสาเหตุยากมาก เพราะหน้าเว็บทำงานได้ปกติทุกอย่าง
 *
 * ไฟล์นี้ถูกโหลดตอน runtime จึงแก้แล้วรีเฟรชเห็นผลทันที
 *
 * วิธีใช้
 * -------
 * ปกติ "ไม่ต้องแก้อะไรเลย" — เว้นทุกช่องว่างไว้ ระบบจะใช้ค่าจาก .env ที่ build มา
 * จะกรอกก็ต่อเมื่อต้องการทับค่าที่ build มาแล้ว เช่น
 *   • ย้ายโดเมนหรือย้ายเซิร์ฟเวอร์ API
 *   • แจกไฟล์ที่ build แล้วให้คนอื่นเอาไปตั้งค่าเอง
 *
 * ⚠️ ค่าในไฟล์นี้เปิดเผยต่อสาธารณะเหมือนกับ NEXT_PUBLIC_* ทุกประการ
 *    ห้ามใส่รหัสฐานข้อมูล, API key หรือความลับใด ๆ เด็ดขาด
 *
 *    (เวอร์ชันก่อนหน้าเคยมีช่อง NEON_DATABASE_URL อยู่ตรงนี้ ซึ่งเป็นความผิดพลาด
 *     — หน้าเว็บไม่ต่อฐานข้อมูลเองอีกแล้ว ทุกอย่างวิ่งผ่าน API_URL ข้างล่างนี้)
 * ========================================================================== */

window.__APP_CONFIG__ = {
  // LIFF ID ของแอปที่ endpoint ชี้มายังเว็บชุดนี้
  // (ดูได้ที่ LINE Developers Console → LINE Login channel → LIFF)
  LIFF_ID: '',

  // ที่อยู่ของ Bun API Server — ใส่ได้ 3 แบบ
  //
  //   'auto'                      ← ใช้เมื่อ Bun server เสิร์ฟทั้งหน้าเว็บและ API
  //                                 จากที่เดียวกัน ระบบจะอ่านที่อยู่จากตำแหน่งของ
  //                                 หน้าเว็บเอง ย้ายโฟลเดอร์ไป path ไหนก็ทำงานต่อ
  //                                 ได้ทันที ไม่ต้องแก้อะไรและไม่ต้อง build ใหม่
  //
  //   'https://xxxx.supabase.co/functions/v1/line-webhook'
  //                               ← ที่อยู่ของ Supabase Edge Function
  //                                 (อย่าลืมตั้ง ALLOWED_ORIGINS ใน Secrets ของฟังก์ชัน)
  //
  //   ''                          ← ใช้ค่าที่ build มาจาก .env
  //
  // ⚠️ ห้ามใส่ที่อยู่ของ "หน้าเว็บ" ถ้าที่นั่นไม่มี Bun server รันอยู่
  //    จะได้หน้า 404 กลับมาเป็น HTML แล้วทุกปุ่มจะล้มพร้อมกัน
  API_URL: '',

  // ที่อยู่ของโปรเจกต์ Supabase — ใช้ประกอบลิงก์รูปในเมนูจัดการรูปภาพ
  //
  // ปกติ "เว้นว่างไว้" เพราะระบบเดาจาก API_URL ให้เองได้
  // (https://xxxx.supabase.co/functions/v1/line-webhook → https://xxxx.supabase.co)
  //
  // กรอกเมื่อผูกโดเมนของตัวเองไว้กับ Edge Function จนเดาไม่ได้
  // เช่น 'https://xxxx.supabase.co'
  SUPABASE_URL: ''
};
